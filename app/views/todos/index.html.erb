<h1>Todo一覧</h1>

<%= link_to "新しいTodoを追加", new_todo_path %>

<ul>
  <% @todos.each do |todo| %>
    <li>
      <%= todo.title %>
      （<%= todo.completed ? "完了" : "未完了" %>）
    </li>
  <% end %>
</ul>
